{{ ansible_managed | comment }}
apiVersion: kubeadm.k8s.io/v1alpha1
kind: NodeConfiguration
masterAddresses:
  - {{ hostvars[groups['master'][0]]['ansible_default_ipv4']['address'] }}
secrets:
  givenToken: {{ hostvars[groups['master'][0]]['kubeadm_token'] }}
